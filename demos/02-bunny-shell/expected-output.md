# Expected output

- `bunny-shell status` says `desktopUsable: true`; Bunny/broker state is `available` only with actual evidence.
- GDM lists Bunny, base GNOME, and Bunny (Safe Shell).
- Launcher app results start validated desktop entries; consequential system intent pauses for confirmation.
- Search has zero locations until approved and never contains file content.
- Workspace archive/detach never deletes files.
- Command proposals never execute.
- Locked notifications reveal no body/action/private prompt/file content.
- Bunny unavailable leaves apps, Files, Terminal, Settings, workspaces, updates, and recovery usable.
- Broker unavailable disables privileged actions without fallback.
- Safe Shell has no Bunny extension behavior.

Any missing image/interactive evidence remains `untested`, not PASS.
