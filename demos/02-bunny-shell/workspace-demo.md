# Workspace demo

```text
bunny-workspace create "Bunny OS" --project "$PWD"
bunny-workspace list
```

Open the Workspaces surface, attach a Bunny thread if the signed artifact is available, archive and restore the workspace, then detach the project. Confirm repository files remain unchanged. Opening the dashboard must not run project scripts.
