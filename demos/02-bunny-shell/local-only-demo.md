# Local-only and offline demo

Open Bunny Settings → Privacy/Bunny. Enable local-only and verify provider alias `local`, cloud failover `never`, local status, and that OS update status remains available. Separately enable offline mode and verify external Bunny/plugin/update checks pause while loopback services and normal applications remain. Capture network evidence; a UI label alone is insufficient.
