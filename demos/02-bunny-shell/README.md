# Bunny Shell demonstration

This package demonstrates the Phase 2 source on a validated Fedora 44 shell image. It does not convert the current Windows host into boot evidence. Start with `build-image.md` and `run-qemu.md`; compare every step to `expected-output.md`. If the signed Bunny artifact is absent, use the documented degraded-path expectations rather than simulating task/approval success.
