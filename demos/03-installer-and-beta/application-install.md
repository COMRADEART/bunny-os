# Application install

Opt in to a reviewed Flatpak remote, install an application per-user in GNOME Software, inspect portal permissions, revoke one enforceable permission, update, and remove. Show a native package permission as `Not enforced by this package format`. Repeat offline to show a clear remote-unavailable failure without affecting the OS.

