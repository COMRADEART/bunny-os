# Dual-boot simulation

Use the synthetic Windows UEFI fixture for source tests and a separately prepared disposable Windows VM image for runtime tests. Show preserved ESP/Windows partitions, unallocated-space plan, warnings for BitLocker/Fast Startup/hibernation, no resize, and both firmware entries after install. Never call the JSON fixture a Windows installation result.

