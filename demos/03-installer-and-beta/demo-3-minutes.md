# Three-minute demo

1. Run `make gate-phase-3` and show the passing static count.
2. Parse the Windows/removable fixtures and show redacted IDs, installation-media exclusion, and existing OS warnings.
3. Preview an encrypted erase plan and show LUKS2 recovery requirement, exact disk identity, and `writesPerformed=false`.
4. Show first-run privacy defaults and a Flatpak/native permission comparison.
5. End on `docs/KNOWN_ISSUES.md`: no disk/image/VM claim was made on this host.

