# Rollback demo

After a successful Phase 2-to-3 update, show current and previous bootc deployments, preserve user files/applications/workspaces, select the previous deployment, reboot, and verify recovery remains available. A Bunny checkpoint is not an OS rollback. Record schema compatibility and any state that cannot be reversed.

