# Automated disposable-disk test

Automated install is disabled in the source build. A future test adapter must validate `schemas/automated-install.schema.json`, require `enabled`, `testEnvironment`, exact `BUNNY_OS_DISPOSABLE_TEST_DISK` confirmation, virtual classification, disk ID and size binding, and a protected secret channel. Show rejection of a physical, removable, mounted, or installation-media target.

