# Phase 5 stable-qualification demonstrations

These are safe source/operations demonstrations. They use synthetic local exports and disposable fixtures only. Artifact, signing, VM, hardware, accessibility, and soak steps stop when evidence is absent. Never substitute the demonstrations for a stable `GO` decision.

Start with `demo-5-minutes.md`; the 15/30-minute variants add reproduction and qualification context. Expected current result: `make gate-phase-5` passes source checks while both protected stable gates remain blocked.
