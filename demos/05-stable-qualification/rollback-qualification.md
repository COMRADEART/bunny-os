# Rollback qualification

Run `make test-rollbacks` to demonstrate content-free preservation comparisons. A qualification run must boot the previous deployment after injected kernel/graphics/service failures and compare all protected datasets. Current runtime status is NOT RUN.
