# Reproduce an issue

Use a fresh q35 UEFI VM and record candidate hash, kernel, firmware, disk/encryption, exact steps, expected/actual result, journal, and redacted logs. Never attach a host disk. Link the failure signature and regression test; leave status unverified when the VM/toolchain is unavailable.
