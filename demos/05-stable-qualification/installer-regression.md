# Installer regression

Run `make test-installer-regressions`. Show monotonic states, mandatory validation, non-resumable destructive failure, explicit irreversibility, and redacted export. Explain that this test does not partition a disk or qualify Anaconda.
