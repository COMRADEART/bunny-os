# Stable gate

Run `make gate-phase-5`, then `make gate-stable-candidate` and `make gate-stable-release`. Source operations should pass; candidate/release should block on absent artifacts/evidence/approvals. Never change unknown statuses merely to make a gate green.
