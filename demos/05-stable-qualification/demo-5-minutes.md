# Five-minute demo

1. Read `docs/PHASE_5_BASELINE.md` and show unknown beta evidence.
2. Run `make test-installer-regressions test-update-regressions test-privacy-regressions`.
3. Run `make release-dashboard` and show categorical NO-GO status without a score.
4. Run `make gate-phase-5`.
5. Run `make gate-stable-candidate`; show the expected missing-manifest block.
