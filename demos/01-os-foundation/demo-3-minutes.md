# Three-minute demo

1. Show provenance/SBOM/checksum and QEMU settings (20s).
2. Boot/login; show GNOME and conventional terminal (40s).
3. Run `bunny-os --json status` and `bunny-os-info --json` (40s).
4. Show denied invalid broker method and metadata-only audit (35s).
5. Show `bootc status`, recovery availability, firewalld/no listeners, and Bunny artifact status (45s).

If using the placeholder, end by showing its explicit refusal and say runtime integration awaits a signed upstream Linux artifact.

