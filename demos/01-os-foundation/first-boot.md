# First boot

1. Complete GNOME Initial Setup: locale, keyboard, timezone, Linux user, hostname, network.
2. Show Bunny OS first-boot defaults: telemetry/diagnostics/cloud fallback off, update check opt-in, Desktop launch opt-in, model setup deferred.
3. Demonstrate offline login, terminal, settings, `bunny-os version`, and `bunny-os hardware`.
4. If the image has the placeholder, launch Bunny and show the explicit artifact-unavailable message. Do not call it a Bunny startup pass.

