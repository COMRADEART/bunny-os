# Recovery demonstration

```text
bunny-os recovery status
bunny-os recovery schedule recovery
bunny-os power reboot
```

At the physical VM console show the Bunny-independent recovery menu, version/deployment and filesystem inventory, confirmed rollback selection, Bunny autostart/plugin disabling in a disposable user fixture, and local diagnostic export. Reboot normally and show the one-shot marker was removed.

