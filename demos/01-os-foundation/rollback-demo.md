# Rollback demonstration

With two real deployments:

```text
bunny-os rollback list
bunny-os rollback select
bootc status
```

Show that the previous deployment becomes selected and requires reboot. Also show the older manifest sequence is rejected as `rollback_attack`. Do not demonstrate rollback against a single-deployment fixture.

