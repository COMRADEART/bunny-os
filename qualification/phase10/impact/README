Per-artifact impact records land here as <artifact>.json, generated by tools/candidate_ops.py build_impact() against the parent artifact through component-domains.json. Empty (beside the mapping) = no artifact beyond ROOT exists = nothing has an impact to record. A dry run may model CANDIDATE-NEXT inside a test; a modelled candidate never gets a file here.
