# Stable evidence report

Date: 2026-07-30
Candidate commit: `79bb99ddb39d8a5dbc279629f43b23346fb0e5e8`
HEAD: `80df25b09f6578276d18c8a82f15c47dd8959740`
Result: **20 records across 20 categories. 2 passing, 18 blocking. 18 categories
unsatisfied. 14 candidate prerequisites, 2 satisfied.**

Generated by `python scripts/release.py stable-evidence-report`. The record
itself is generated by `scripts/build_evidence_record.py`, which computes every
content digest from the file on disk — no digest in this record was typed by
hand.

## A bug in the commit binding, and the fix

At the start of the qualification evidence closure this report said **0 passing and
20 missing**, not 2 and 18. Every one of the twenty records failed with:

```text
generated from commit 79bb99ddb39d but the candidate is 80df25b09f65;
evidence does not transfer between commits
```

The rule was correct and the caller was wrong. Evidence was being compared against
`HEAD`, so **committing the record changed `HEAD` and invalidated the record in the
same act.** Two categories that genuinely passed reported as stale for no reason
but being written down.

`operations/data/release-evidence.json` now declares a **`candidateCommit`** — the
commit that was built and measured — and the gate compares against that. Wrong-commit
evidence still blocks and is tested. Whether the candidate is still `HEAD` is
reported separately, because qualifying an older commit is legitimate for a release
candidate and must nevertheless be visible:

```text
candidate commit: 79bb99ddb39d, HEAD: 80df25b09f65
  Evidence is bound to the candidate commit, not to HEAD. A candidate behind HEAD is
  legitimate — a release candidate qualifies one commit — but the tree has moved
  since the evidence was measured and a rebuild is needed before publication.
```

The record now reports its true position again.

## The record model

Every requirement carries: id, category, description, evidence type, evidence
reference, generated time, source commit, result, reviewer, optional expiry,
waiver status and blocker status.

Four ways a record can lie are checked rather than trusted:

| Failure | Check |
|---|---|
| **Forgery** | the record carries the artifact's SHA-256; the gate recomputes it. A record naming a missing file, a changed file, or no digest at all blocks. |
| **Staleness** | a record past `expiresAt` blocks, whatever its result. |
| **Wrong commit** | a record generated from a different commit than the **candidate** blocks. Evidence does not transfer between commits. |
| **Self-review** | a `third-party-report` or `physical-hardware-report` from a reviewer not registered as independent blocks. |

`UNKNOWN`, `NOT_RUN` and `BLOCKED` all block, as does a category with no record.
There is no code path that treats absence as success.

**Waivers are recorded, never applied.** Accepting one is a release-approver
decision, so a waived record stays blocking and the waiver surfaces in the
report. Six categories — Encryption, Signing, Recovery, Privacy, Accessibility,
Multi-user — cannot carry a waiver at all; the parser rejects it.

## The twenty categories

| Category | Result | Why |
|---|---|---|
| Build | **PASS** | beta built from pinned base `sha256:fb71f099`, archive normalised, reproduced identically in two workspaces |
| Licence | **PASS** | owner decision applied; 6077 SPDX records, 0 unresolved, no prohibited markers |
| Signing | **FAIL** | no production key of any role exists; the development drill cannot substitute |
| Vulnerability | **FAIL** | 59 fixable, 8 Critical, 28 High; 24 unique Critical/High pairs dispositioned `Unknown` |
| Installer | NOT_RUN | the install harness is interactive; no live ISO built |
| Encryption | NOT_RUN | depends on a completed installation |
| Secure Boot | NOT_RUN | requires physical firmware |
| Update | NOT_RUN | no signed update manifest has been published |
| Rollback | NOT_RUN | no previous release to roll back to |
| Recovery | NOT_RUN | no signed recovery ISO exists |
| Migration | NOT_RUN | requires two published releases |
| Multi-user | NOT_RUN | source tests pass; no installed system exercised |
| Local-only | NOT_RUN | requires an installation |
| Bunny-disabled | NOT_RUN | requires an installation |
| Privacy | NOT_RUN | a booted-image capture exists; no installed-system capture, no independent review |
| Accessibility | NOT_RUN | zero of fourteen workflows driven with assistive technology |
| Hardware | NOT_RUN | zero reports; needs a device, not more tests |
| Performance | NOT_RUN | no installed system to measure |
| Soak | NOT_RUN | requires an installed candidate and elapsed time |
| Support | NOT_RUN | one maintainer, no funded rota, no second signer |

## The candidate prerequisite dashboard

Twenty evidence categories answer *what has been measured*. Fourteen candidate
prerequisites answer *what stops an artifact being called release-qualified*, and
they are new. The dashboard is generated from the same evaluation as this report, so
the two cannot disagree.

```text
candidate prerequisites: 2 of 14 satisfied
  BLOCKED                  independent-reproducibility, second-production-signer
  NOT_RUN                  encryption-matrix, independent-recovery-media,
                           installation-matrix, rollback-matrix, update-matrix
  PASS                     development-signing-drill, licence-gate
  PENDING_EXTERNAL_REVIEW  accessibility-evidence, independent-reviews,
                           vulnerability-gate
  PENDING_HARDWARE         physical-hardware-evidence
  PENDING_OWNER            protected-approvals
```

Eight states, and only `PASS` satisfies anything. The four `PENDING_*` states are
not softer than `BLOCKED` — they name **who can resolve it**, which `BLOCKED` alone
does not. Full rows, with owner, evidence, commit, age, blocker, next action and
dependency, in `build/out/qualification/stable-evidence-dashboard.md`.

**No aggregate percentage is shown.** A completion figure reads as nearly done when
what remains needs a third party, and a test asserts the dashboard contains no `%`.

## What changed this phase

Two categories moved from blocking to passing:

- **Build.** Previously there was no committed build evidence. There is now a
  beta image built from a digest-pinned base, with a normalised archive that two
  isolated workspaces reproduced byte-for-byte.
- **Licence.** Previously the highest-priority licence gap in
  `LICENSE_COMPLIANCE_REPORT.md`. The owner decided, and the root `LICENSE`,
  eight per-directory files, 127 SPDX headers and a clean scan followed.

**Signing** moved from absent to explicitly `FAIL`, which is a real improvement:
the row now states that no production key exists rather than leaving the
question open.

## The shape of what remains

Eighteen blocking categories, by what would resolve them:

| Needs | Categories |
|---|---|
| A live ISO and a recovery ISO, then VM runs | Installer, Encryption, Recovery |
| A published manifest and a previous release | Update, Rollback, Migration |
| An installation to exercise | Multi-user, Local-only, Bunny-disabled, Privacy, Performance, Soak |
| **Physical hardware** | Hardware, Secure Boot |
| **An independent third party** | Accessibility, and the route to dispositioning Vulnerability |
| **A key ceremony and a second signer** | Signing |
| **A funded support commitment** | Support |

The first two rows are engineering. The last four are not, and no amount of
further work in this repository moves them.

## Adversarial coverage

`tests/release/test_evidence_model.py` proves the model rejects: a record naming
a missing artifact, a record whose artifact was substituted after the fact, a
record with no digest, a reference escaping the repository, an expired record, a
record from another commit, and a third-party claim from an unregistered
reviewer. It also proves a waiver does not clear a blocking result, and that a
non-waivable category refuses a waiver outright.

## Regenerating

```text
python scripts/build_evidence_record.py
python scripts/release.py stable-evidence-report
make stable-evidence-report
```

**After a rebuild, update `candidateCommit` to the rebuilt commit.** The record
describes the commit that was built; leaving it behind is how evidence goes quietly
stale.

Full JSON: `build/out/qualification/stable-evidence-report.json`.
Dashboard: `build/out/qualification/stable-evidence-dashboard.md`.
Candidate detail: `build/out/qualification/qualification-candidate.json`.
Source record: `operations/data/release-evidence.json`.

## Related

- `QUALIFICATION_CANDIDATE_READINESS_REPORT.md` — the fourteen prerequisites in full
- `QUALIFICATION_EVIDENCE_CLOSURE_REPORT.md` — what this phase built and did not
- `docs/QUALIFICATION_EVIDENCE_BASELINE.md` — every unmet requirement classified by
  who can produce it

## 2026-07-30 addendum: the reproducibility row moved from absent to measured

The `Build` evidence category and the `independent-reproducibility` candidate
prerequisite both depended on a hosted builder run that had never happened. It
has now happened, and the result is recorded rather than assumed.

| | before | after |
|---|---|---|
| Hosted builder records | 0 | 1 (`hosted-ci-30566412012`) |
| Local builder records (schema 2) | 0 | 1 (`local-fedora-wsl`) |
| Declared independence pairs | 0 | 1 |
| Comparison dimensions collected | 1 of 17 | 16 of 17 |
| Comparison outcome | `INCONCLUSIVE` (nothing measured) | `NON_REPRODUCIBLE` (measured) |
| `independent-reproducibility` | `BLOCKED` | `BLOCKED` |

The prerequisite state did not change. What changed is why. It was blocked
because no second builder existed; it is blocked because the second builder exists
and the comparison it made possible reported `NON_REPRODUCIBLE` — eleven
dimensions matching, five differing, one not collectable from an archive-only
build.

Fifteen files differ out of 104,247, all build-environment state. The package
inventory (6,076 packages) and the kernel match exactly. Details in
`INDEPENDENT_REPRODUCIBILITY_REPORT.md`.

The candidate prerequisite count is unchanged at **2 of 14**:
`licence-gate` and `development-signing-drill`.

### 2026-07-30, later: the comparison was rerun and it found the apparatus wrong

Two further passes were made. The second reduced the fifteen differing files to
two. The third measured *why* those two differed instead of inferring it, and the
inference had been wrong: the databases were structurally identical and differed
in fifty rows of content, from a build clock that was offset rather than frozen.

Three defects in the evidence apparatus were found in the process, and they
matter more to this report than the build fixes do:

* **The input locks were untracked.** Every "fresh clone" comparison had been run
  with the pins hand-copied in. A hosted builder has nothing but the commit.
* **Both builds shared a layer cache.** The second build could have been served
  the first one's layers, which would have produced a byte-identical archive
  because it *was* the first archive.
* **The dimension collector did not compare entry mtimes**, so an archive
  difference caused by a wall-clock mtime had no file to attribute it to and was
  attributed to the databases instead.

None of these changed a gate. All three changed what a passing comparison would
have been worth.

`independent-reproducibility` remains `BLOCKED`, and now for a fourth distinct
reason: the retained inputs are not published, so there is no second builder that
can obtain them. That is one token scope, named in
`PACKAGE_INPUT_PUBLICATION_REPORT.md`.

The candidate prerequisite count is unchanged at **2 of 14**.

### The evidence commit is not the candidate commit

This addendum is committed after `9ea5459bdaf122f8c5999683b2c8961555826954`, the
commit both builders built. `operations/data/release-evidence.json` still
declares its own `candidateCommit` for the scan-derived evidence it covers, and
neither was moved to the commit that imported these reports.

Promoting an evidence-import commit to candidate would claim that evidence
describes a tree which did not exist when the evidence was measured.
`release/commits.py` distinguishes the two, and
`tests/portability/test_hosted_import.py` asserts the committed candidate is
never `HEAD`.

## Addendum — 2026-08-01, TPM boot-reset investigation

The software-TPM boot category moves from a measured `FAIL` to a passing
software-TPM regression under the dedicated `tpmq-1` authority
(`TPM_QUALIFICATION_REPORT.md`); the root cause was a harness
misinterpretation of shim fallback's designed restoration reboot, confidence
`CONFIRMED` (`TPM_GRUB_RESET_ROOT_CAUSE.md`). No artifact byte changed, so
no archive-bound evidence in this report is disturbed, and nothing here is
relabelled: the invalidated `ISQ-20260801-tpm-present-*` records remain in
the tree as invalidated harness evidence.

Software-TPM boot is not one of the fourteen candidate prerequisites, so the
candidate count is whatever the gate recalculates from evidence — this
addendum predicts nothing. Physical TPM evidence remains `NOT_RUN`, and the
importer that produced the TPM verdict emits that state unconditionally: no
software-TPM record can move a physical claim.
