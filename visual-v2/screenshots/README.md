# Visual V2 screenshot scenarios

> VISUAL PROTOTYPE ONLY
>
> NOT RELEASE QUALIFIED
>
> DO NOT MERGE INTO MAIN

The 26 deterministic SVGs under `rendered/` are visual-review artifacts. Every
render is labelled `VISUAL MOCK DATA`; none is functional, qualification, or
release evidence. Regular scenarios contain no character image element.

Run `python visual-v2/tools/render_screenshots.py --check` to verify that the
tracked renders match their scenario and token sources.
