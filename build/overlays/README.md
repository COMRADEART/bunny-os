# Root overlays

Phase 1 installs owned files through `build/scripts/install-root.py` so modes and destinations are explicit. Profile-specific static overlays may be added here only when they cannot be represented by the source directories and must be covered by image inspection tests.

