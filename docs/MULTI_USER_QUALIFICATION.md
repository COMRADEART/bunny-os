# Multi-user qualification

Use two regular users plus a separate administrative user. Verify independent Bunny memory, credential references, plugins, workspaces, state directories, portals, diagnostics, and lock screens; shared applications/models require explicit configuration and permissions. Exercise simultaneous sessions where supported.

Any cross-user Bunny data or credential exposure is a stable blocker. Source permissions are not sufficient evidence; no installed multi-user run exists.
