# Release dashboard

`make release-dashboard` creates a local evidence table for candidate/build/test/blocker/security/privacy/accessibility/installer/update/rollback/recovery/hardware/docs/signing/artifact status. It displays categorical evidence and links, never a fabricated percentage or composite quality score.

The tracked qualification source currently produces `NO-GO`. Dashboard output is informational; the protected candidate/release gates and signed evidence remain authoritative.
