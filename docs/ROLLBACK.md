# Rollback

No supported stable rollback path exists yet. The design preserves the previous bootc deployment, but persistent user and `/var` data do not automatically revert. Before a future update, the UI must display migration/downgrade limits and confirm a tested previous deployment/recovery path.

Use only release-specific instructions and verified recovery media. Never manually overwrite deployments or user data based on this source-only guide.
