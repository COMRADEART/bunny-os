# Visual demo

`storyboard.json` is the deterministic review sequence. `record-demo.sh`
captures an actual nested Wayland run with `wf-recorder`; it never replaces the
developer's current session. Recordings go to ignored `build/visual/demo/`.

No recording is committed from a non-GNOME development host. A generated mock
animation would not be presented as an actual shell demonstration.
