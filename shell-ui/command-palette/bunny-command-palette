#!/usr/bin/env python3
"""BUNNY WAYLAND SHELL EXPERIMENT - NOT RELEASE QUALIFIED - DO NOT USE AS THE DEFAULT SESSION."""
from pathlib import Path
import sys

source = Path(__file__).resolve().parents[2] / "apps/common"
installed = Path("/usr/lib/bunny-shell-v3")
sys.path.insert(0, str(source if source.is_dir() else installed))

from bunny_shell_v3.component_main import main

raise SystemExit(main("command-palette"))
