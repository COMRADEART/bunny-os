#!/usr/bin/env python3
from pathlib import Path
import sys

source = Path(__file__).resolve().parents[2] / "apps/common"
installed = Path("/usr/lib/bunny-visual-v2")
sys.path.insert(0, str(source if source.is_dir() else installed))

from bunny_visual_v2 import run_surface

raise SystemExit(run_surface("control-center"))
